#include <stdio.h>

int main() {
    int x = 10;

    printf("oi\n");
    printf("o valor de x e %d.\n", x); 
    
/*
%d para inteiros, %f para números de ponto flutuante, %s para cadeias de caracteres (strings), %ld para long, %lf para double, %c para char
%u para unsigned, %e para notacao cientifica
a substituição é feita da esquerda para a direita
*/
return 0;
}