#include <string.h>
//strlen(string); retorna o tamanho de uma string
//strcpy(string1, string2); copia a string2 na string1, strncpy(string1, string2, n)
//strcat(string1, string2); concatena duas strings, strncat(string1, string2, n); concatena os primeiros n caracteres


//strcmp(string1, string2); compara duas strings, já que não podemos usar <, <=, >, >=, etc
//0 caso as duas strings sejam iguais
//menor que 0 caso string1 seja menor que string2
//maior que 0 caso string1 seja maior que string2