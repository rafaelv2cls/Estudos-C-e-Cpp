#include <stdio.h>

int main(){
    int a = 1; 
    int x;
    x = (a++) * 2; // a++ é a mesma coisa que a = a + 1, porém depois de ser usado, ++a é primeiro incrementado depois usado

return 0;
}